package class_design;

/*
 * we will learn : 
 * -how to implement interface
 */

public class implementing_interface {
	
	/*/*
	 * -refer code at class_inheritance.abstract_classes at HasTail.java
	 * and Dog.java
	 * -class needs to be abstract in order to implement interface
	 * -interface must be implemented anywhere in the hierarchy
	 * -1 class can implement multiple interface
	 * -child/parent class must implement method that parent class implements,
	 * unless there is default method that has been declared
	 * -interface can also extend other interface
	 */

}
